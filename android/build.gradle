// Root build.gradle for Baby Care App (Flutter Embedding v2 + AGP 8.x)

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}
